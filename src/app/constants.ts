const API_URL = "http://localhost:8080";

enum Mode {
    Dark = "dark",
    Light = "light"
}

enum SortOrder {
    Ascending = 'ascending',
    Descending = 'descending',
}

export { API_URL, Mode, SortOrder };
